#---------------------------------------------| @mod
function add(x,y) {
    return x + y;
}

module.exports = {
    add
}
#---------------------------------------------| @src
xs: [1,2,3,4,5],
add: function(a, b) {
    return a - b;
}
#---------------------------------------------| @func hello x
HELLO: ${x}
#---------------------------------------------| @func range min max
${min}${hello(min)}
#--------------------------| min < max
${$func.range(min+1,max)}
#---------------------------------------------|
${$func.range(1,10)}
#---------------------------------------------| @foreach xs
[${$}]${add($,1)}
#----------------| $ == 2
ok${L("OK")}
#----------------|
[${$}]