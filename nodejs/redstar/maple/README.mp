#---------------------------------------------| @src
xs:[[1,2,3],[4,5,6]],
xa: "xa1",
xb: "xb1",
xc: "xc2",
#------------|
ys:[],
name: "ABCDEFG",
#---------------------------------------------| @func add x y
${x}+${y} = ${x+y}
#---------------------------------------------| @foreach x:xs
${$key}:${x}
#--------------------------| @foreach x
    - ${$key}:${$}
#--------------| $ % 2 === 0
        - ${$} is ODD : ${add($,100)}
#---------------------------------------------| @foreach xs
${$.join('--')}
#---------------------------------------------| @foreach name
${$}
#---------------------------------------------| @zsh
ls -al /
#---------------------------------------------| @foreach _.range(1,10000)
name ${$} is ${$} years old !
#---------------------------------------------| @foreach [4,3,2,1]
${$}
#---------------------------------------------| @foreach x:["${xa}", "${xb}"]
${x}