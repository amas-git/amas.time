#---------------------------------------------| @src
xs:[[1,2,3],[4,5,6]],
#------------|
ys:[],
name: "ABCDEFG"
#---------------------------------------------| @func add x y
${x}+${y} = ${x+y}
#---------------------------------------------| @foreach x:xs
${$key}:${x}
#--------------------------| @foreach x
    - ${$key}:${$}
#--------------| $ % 2 === 0
        - ${$} is ODD : ${add($,100)}
#---------------------------------------------| @foreach xs
${$.join('--')}
#---------------------------------------------| @foreach name
${$}
#---------------------------------------------| @zsh
ls -al /